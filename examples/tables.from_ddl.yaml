# Draft tables.yaml generated by local/scripts/ddl_to_yaml.py.
# Resolve every TODO before using this in production.

defaults:
  source_schema: TODO  # the source schema name; matches data_mart in the local example
  write_mode: append
  fetch_size: 5000
  glue_database_prefix: silver_

tables:

  # ----- DIM_ACCOUNT -----
  # columns: id:bigint | data_owner_id:bigint | code:varchar(255) | name:varchar(255) | description:varchar(255) | account_number:varchar(255) | currency:varchar(3) | currency_group_code:varchar(255) | ...
  #   ... and 56 more
  DIM_ACCOUNT:
    source_table: dim_account
    domain: TODO_domain  # one of: shared, payments, balances, etc.
    watermark_column: last_updated_time
    primary_key: [id]
    partition_spec: []  # dim_ prefix — assumed small/reference, no partitioning needed
    # sort_order: TODO_sort  # optional Iceberg sort; depends on query patterns

  # ----- DIM_DATE -----
  # columns: id:bigint | cal_date:date | cal_year:smallint | cal_month:varchar(3) | cal_day:smallint | day_of_week:varchar(3) | day_of_year:smallint | week_of_year:smallint | ...
  #   ... and 1 more
  DIM_DATE:
    source_table: dim_date
    domain: TODO_domain  # one of: shared, payments, balances, etc.
    watermark_column: TODO_watermark  # no column matched the heuristic; pick a monotonically-increasing timestamp
    primary_key: [id]
    partition_spec: []  # dim_ prefix — assumed small/reference, no partitioning needed
    # sort_order: TODO_sort  # optional Iceberg sort; depends on query patterns

  # ----- DIM_AS_CHARACTERISTICS -----
  # columns: id:bigint | balance_opening_flag:char(1) | balance_closing_flag:char(1) | balance_closing_available_flag:char(1) | transaction_intraday_flag:char(1)
  DIM_AS_CHARACTERISTICS:
    source_table: dim_as_characteristics
    domain: TODO_domain  # one of: shared, payments, balances, etc.
    watermark_column: TODO_watermark  # no column matched the heuristic; pick a monotonically-increasing timestamp
    primary_key: [id]
    partition_spec: []  # dim_ prefix — assumed small/reference, no partitioning needed
    # sort_order: TODO_sort  # optional Iceberg sort; depends on query patterns

  # ----- DIM_CURRENCY -----
  # columns: id:bigint | code:varchar(20) | name:varchar(150) | numeric_code:varchar(3) | number_of_decimals:int | major_unit_name:varchar(70) | minor_unit_name:varchar(70) | is_default_currency:char(1) | ...
  #   ... and 1 more
  DIM_CURRENCY:
    source_table: dim_currency
    domain: TODO_domain  # one of: shared, payments, balances, etc.
    watermark_column: last_updated_time
    primary_key: [id]
    partition_spec: []  # dim_ prefix — assumed small/reference, no partitioning needed
    # sort_order: TODO_sort  # optional Iceberg sort; depends on query patterns

  # ----- DIM_DATA_OWNER -----
  # columns: id:bigint | code:varchar(255) | name:varchar(255) | country_code:varchar(2) | country_name:varchar(255) | last_updated_time:timestamp
  DIM_DATA_OWNER:
    source_table: dim_data_owner
    domain: TODO_domain  # one of: shared, payments, balances, etc.
    watermark_column: last_updated_time
    primary_key: [id]
    partition_spec: []  # dim_ prefix — assumed small/reference, no partitioning needed
    # sort_order: TODO_sort  # optional Iceberg sort; depends on query patterns

  # ----- DIM_USER -----
  # columns: id:bigint | code:varchar(255) | name:varchar(255) | email:varchar(255) | last_updated_time:timestamp
  DIM_USER:
    source_table: dim_user
    domain: TODO_domain  # one of: shared, payments, balances, etc.
    watermark_column: last_updated_time
    primary_key: [id]
    partition_spec: []  # dim_ prefix — assumed small/reference, no partitioning needed
    # sort_order: TODO_sort  # optional Iceberg sort; depends on query patterns

  # ----- DIM_CLASSIFICATION -----
  # columns: id:bigint | code:varchar(255) | name:varchar(255) | last_updated_time:timestamp
  DIM_CLASSIFICATION:
    source_table: dim_classification
    domain: TODO_domain  # one of: shared, payments, balances, etc.
    watermark_column: last_updated_time
    primary_key: [id]
    partition_spec: []  # dim_ prefix — assumed small/reference, no partitioning needed
    # sort_order: TODO_sort  # optional Iceberg sort; depends on query patterns

  # ----- DIM_ROUTING -----
  # columns: id:bigint | source_system:varchar(50) | source_channel:varchar(50) | target_system:varchar(50) | target_channel:varchar(50)
  DIM_ROUTING:
    source_table: dim_routing
    domain: TODO_domain  # one of: shared, payments, balances, etc.
    watermark_column: TODO_watermark  # no column matched the heuristic; pick a monotonically-increasing timestamp
    primary_key: [id]
    partition_spec: []  # dim_ prefix — assumed small/reference, no partitioning needed
    # sort_order: TODO_sort  # optional Iceberg sort; depends on query patterns

  # ----- DIM_AS_TRANSACTION_TYPE -----
  # columns: id:bigint | domain:varchar(7) | family:varchar(7) | sub_family:varchar(7) | proprietary_code:varchar(35) | proprietary_issuer:varchar(35)
  DIM_AS_TRANSACTION_TYPE:
    source_table: dim_as_transaction_type
    domain: TODO_domain  # one of: shared, payments, balances, etc.
    watermark_column: TODO_watermark  # no column matched the heuristic; pick a monotonically-increasing timestamp
    primary_key: [id]
    partition_spec: []  # dim_ prefix — assumed small/reference, no partitioning needed
    # sort_order: TODO_sort  # optional Iceberg sort; depends on query patterns

  # ----- DIM_PAY_CHARACTERISTICS -----
  # columns: id:bigint | letter_company_flag:char(1) | letter_confidential_flag:char(1) | letter_locked_flag:char(1) | letter_signed_flag:char(1) | letter_blocked_flag:char(1) | letter_first_signed_flag:char(1) | letter_second_signed_flag:char(1) | ...
  #   ... and 8 more
  DIM_PAY_CHARACTERISTICS:
    source_table: dim_pay_characteristics
    domain: TODO_domain  # one of: shared, payments, balances, etc.
    watermark_column: TODO_watermark  # no column matched the heuristic; pick a monotonically-increasing timestamp
    primary_key: [id]
    partition_spec: []  # dim_ prefix — assumed small/reference, no partitioning needed
    # sort_order: TODO_sort  # optional Iceberg sort; depends on query patterns

  # ----- DIM_PAY_BANK_STATUS -----
  # columns: id:bigint | accepted_flag:char(1) | rejected_flag:char(1) | accepted_flag_clearing:char(1) | rejected_flag_clearing:char(1)
  DIM_PAY_BANK_STATUS:
    source_table: dim_pay_bank_status
    domain: TODO_domain  # one of: shared, payments, balances, etc.
    watermark_column: TODO_watermark  # no column matched the heuristic; pick a monotonically-increasing timestamp
    primary_key: [id]
    partition_spec: []  # dim_ prefix — assumed small/reference, no partitioning needed
    # sort_order: TODO_sort  # optional Iceberg sort; depends on query patterns

  # ----- FACT_AS_BALANCE -----
  # columns: id:bigint | balance_type_id:bigint | account_id:bigint | currency_id:bigint | balance_date_id:bigint | data_owner_id:bigint | routing_id:bigint | balance_amount:numeric(31,16) | ...
  #   ... and 13 more
  FACT_AS_BALANCE:
    source_table: fact_as_balance
    domain: TODO_domain  # one of: shared, payments, balances, etc.
    watermark_column: last_updated_time
    primary_key: [id]
    partition_spec:
      - { transform: days, column: fx_rate_date }
      # TODO: for large facts add a bucket transform on the PK, e.g.:
      # - { transform: bucket, column: id, n: 16 }
    # sort_order: TODO_sort  # optional Iceberg sort; depends on query patterns

  # ----- FACT_AS_TRANSACTION -----
  # columns: id:bigint | account_id:bigint | currency_id:bigint | value_date_id:bigint | data_owner_id:bigint | intraday_id:bigint | classification_id:bigint | routing_id:bigint | ...
  #   ... and 21 more
  FACT_AS_TRANSACTION:
    source_table: fact_as_transaction
    domain: TODO_domain  # one of: shared, payments, balances, etc.
    watermark_column: last_updated_time
    primary_key: [id]
    partition_spec:
      - { transform: days, column: fx_rate_date }
      # TODO: for large facts add a bucket transform on the PK, e.g.:
      # - { transform: bucket, column: id, n: 16 }
    # sort_order: TODO_sort  # optional Iceberg sort; depends on query patterns

  # ----- FACT_AS_CURRENCY_EXCHANGE -----
  # columns: id:bigint | data_owner_id:bigint | rate_date_id:bigint | from_currency_id:bigint | to_currency_id:bigint | exchange_rate:numeric(28,10) | last_updated_time:timestamp
  FACT_AS_CURRENCY_EXCHANGE:
    source_table: fact_as_currency_exchange
    domain: TODO_domain  # one of: shared, payments, balances, etc.
    watermark_column: last_updated_time
    primary_key: [id]
    partition_spec:
      - { transform: days, column: last_updated_time }
      # TODO: for large facts add a bucket transform on the PK, e.g.:
      # - { transform: bucket, column: id, n: 16 }
    # sort_order: TODO_sort  # optional Iceberg sort; depends on query patterns

  # ----- FACT_PAY_PAYMENT -----
  # columns: id:bigint | ordering_account_id:bigint | requesting_execution_date:bigint | payment_method_id:bigint | currency_id:bigint | data_owner_id:bigint | classification_id:bigint | characteristics_id:bigint | ...
  #   ... and 36 more
  FACT_PAY_PAYMENT:
    source_table: fact_pay_payment
    domain: TODO_domain  # one of: shared, payments, balances, etc.
    watermark_column: last_updated_time
    primary_key: [id]
    partition_spec:
      - { transform: days, column: amount_format_date }
      # TODO: for large facts add a bucket transform on the PK, e.g.:
      # - { transform: bucket, column: id, n: 16 }
    # sort_order: TODO_sort  # optional Iceberg sort; depends on query patterns

